let p: [string, number];
p = ['Programming', 5]; 
console.log(p);


/*
let p1: [string, number];
p1 = [5, 'Programming'];
console.log(p1); */

let color: [number, number, number] = [255, 0, 0];
console.log(color);

let bgColor, headerColor: [number, number?];
bgColor = [0, 255, 255]; //4
headerColor = [0, 255]; //3
console.log(bgColor);
console.log(headerColor);
